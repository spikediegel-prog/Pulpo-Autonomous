[project]
name = "pulpo-independent-authority"
version = "0.1.0"
description = "Independent WebAuthn authority service for Pulpo"
requires-python = ">=3.11"
dependencies = [
  "cryptography==50.0.1",
  "fastapi==0.141.1",
  "uvicorn==0.52.4",
  "webauthn==3.0.0",
]

[project.optional-dependencies]
test = [
  "httpx2==2.12.0",
  # Starlette 1.6.0 still imports anyio.abc.BlockingPortal. AnyIO 4.15.0
  # deprecated that alias, which becomes a hard failure under `python -W error`.
  # Pin the last reviewed compatible line so CI cannot drift underneath an
  # unchanged authority proof.
  "anyio==4.14.2",
]
gcp-auth = ["google-auth==2.57.0"]
gcp-kms = ["google-cloud-kms==3.16.0"]
gcp-storage = ["google-cloud-storage==3.13.1"]
gcp-cloud-sql = [
  "cloud-sql-python-connector==1.22.0",
  "pg8000==1.31.5",
]

[tool.setuptools.packages.find]
where = ["src"]
